<?xml version="1.0" encoding="UTF-8"?>
<?xaml-comp compile="true" ?>

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Style x:Key="InfoCardBorderStyle"
           TargetType="Border">
        <Setter Property="Stroke"
                Value="Purple" />
        <Setter Property="StrokeThickness"
                Value="2" />
        <Setter Property="BackgroundColor"
                Value="#1e1e1e" />
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="0,8,8,0" />
            </Setter.Value>
        </Setter>
        <Setter Property="HeightRequest"
                Value="80" />
        <Setter Property="Padding"
                Value="12" />
    </Style>

    <Style x:Key="InfoCardBorderStyle2"
           TargetType="Border"
           BasedOn="{StaticResource InfoCardBorderStyle}">
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="8,0,0,8" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InfoCardBorderStyle3"
           TargetType="Border"
           BasedOn="{StaticResource InfoCardBorderStyle}">
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="8" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TopLeftRoundedBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource InfoCardBorderStyle}">
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="8,0,0,0" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TopRightRoundedBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource InfoCardBorderStyle}">
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="0,8,0,0" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BottomLeftRoundedBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource InfoCardBorderStyle}">
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="0,0,8,0" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BottomRightRoundedBorderStyle"
           TargetType="Border"
           BasedOn="{StaticResource InfoCardBorderStyle}">
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="0,0,0,8" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Label Styles -->

    <Style x:Key="InfoCardTitleStyle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="HorizontalOptions"
                Value="Start" />
    </Style>

    <Style x:Key="InfoCardValueStyle"
           TargetType="Label">
        <Setter Property="FontAttributes"
                Value="Bold" />
        <Setter Property="FontSize"
                Value="24" />
        <Setter Property="HorizontalOptions"
                Value="Center" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="TextColor" Value="White" />
    </Style>

    <!-- Page Style -->

    <Style TargetType="ContentPage">
        <Setter Property="BackgroundColor" Value="DarkSlateGrey" />
    </Style>

    <!-- Button Style -->

    <Style TargetType="ImageButton">
        <Setter Property="MinimumHeightRequest" Value="44" />
        <Setter Property="MinimumWidthRequest" Value="44" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="Scale"
                                    Value="1" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="Scale"
                                    Value="0.8" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver" />
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="TextColor" Value="White" />
        <Setter Property="BackgroundColor" Value="#FF4B024B" />
        <Setter Property="FontFamily" Value="OpenSansRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderColor" Value="Purple" />
        <Setter Property="BorderWidth" Value="2" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="MinimumHeightRequest" Value="44" />
        <Setter Property="MinimumWidthRequest" Value="44" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#FF8A2BE2" />
                            <Setter Property="BorderColor" Value="#FFDA70D6" />
                            <Setter Property="BorderWidth" Value="4" />
                            <Setter Property="Scale" Value="0.95" />
                            <Setter Property="TextColor" Value="#FFFFFFFF" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor"
                                    Value="{AppThemeBinding Light={StaticResource Gray950}, Dark={StaticResource Gray200}}" />
                            <Setter Property="BackgroundColor"
                                    Value="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#FF6A1B9A" />
                            <Setter Property="BorderColor" Value="#FFBA68C8" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style x:Key="PlainButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="BorderColor" Value="Transparent" />
        <Setter Property="BorderWidth" Value="0" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <!-- Navigation Button Style -->
    <Style x:Key="NavigationButtonBorderStyle"
           TargetType="Border">
        <Setter Property="Stroke" Value="#444444" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="BackgroundColor" Value="#2a2a2a" />
        <Setter Property="StrokeShape">
            <Setter.Value>
                <RoundRectangle CornerRadius="8" />
            </Setter.Value>
        </Setter>
        <Setter Property="HeightRequest" Value="50" />
        <Setter Property="WidthRequest" Value="50" />
        <Setter Property="Padding" Value="8" />
    </Style>

</ResourceDictionary>